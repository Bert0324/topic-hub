/**
 * Single HTTP path segment for the native Topic Hub integration ingress (POST).
 * With Nest `setGlobalPrefix`, the full path is `/${prefix}/${NATIVE_INTEGRATION_SEGMENT}`.
 */
export const NATIVE_INTEGRATION_SEGMENT = 'topic-hub';
